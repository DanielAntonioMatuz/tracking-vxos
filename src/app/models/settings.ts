export class Settings {
  constructor(
    // tslint:disable-next-line:variable-name
    public  _id: string,
    public navbarColor: string,
    public textColor: string,
    public notificationColor: string,
    public fecha: string,
    public logo: string,
    public logoText: string,
    public estatus: string,
    public styleLogo: string,
    public user: string
  ) {
  }
}
